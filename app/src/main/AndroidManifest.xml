<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myblogapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".createblog.AddContent"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".createblog.AddTitle"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".createblog.AddTags"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".createblog.CreateBlog"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.CompleteUserInfo" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Activities.CompleteBlog" />
        <activity android:name=".Activities.Create_Account" />
        <activity android:name=".Activities.BlogFeed" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>